function DisplayProps({name,greeting}){
    //let {name,greeting,emoji}=props;
    return (
        <>
        <h2>This is KM's DisplayProps Component</h2>
        <h4>{greeting} {name}</h4>
        
        </>
    )
}
export default DisplayProps